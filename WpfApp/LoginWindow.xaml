<Window x:Class="WPFApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Đăng nhập hệ thống" 
                   FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1">
            <Label Content="Email:"/>
            <TextBox x:Name="txtEmail" Height="30" Width="250"/>
            <Label Content="Mật khẩu:" Margin="0 10 0 0"/>
            <PasswordBox x:Name="txtPassword" Height="30" Width="250"/>
        </StackPanel>

        <Button Content="Đăng nhập" Grid.Row="2"
                Width="120" Height="35" Margin="0 20 0 0"
                HorizontalAlignment="Center"
                Click="Login_Click"/>
    </Grid>
</Window>
